from django.conf.urls.defaults import *

# Uncomment the next two lines to enable the admin:
# from django_lettuce.contrib import admin
# admin.autodiscover()

urlpatterns = patterns('',
    # Example:
    # (r'^cucumber/', include('cucumber.foo.urls')),

    # Uncomment the admin/doc line below and add 'django_lettuce.contrib.admindocs'
    # to INSTALLED_APPS to enable admin documentation:
    # (r'^admin/doc/', include('django_lettuce.contrib.admindocs.urls')),

    # Uncomment the next line to enable the admin:
    # (r'^admin/', include(admin.site.urls)),
)
